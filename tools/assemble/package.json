{
	"name": "assemble",
	"version": "1.0.0",
	"description": "Assemble an OpenApi 3.1 compatible bundle",
	"author": "Frank Stock",
	"license": "MIT",
	"scripts": {
		"start": "cross-env TS_NODE_PROJECT=tsconfig.json && node --require ts-node/register/transpile-only --require tsconfig-paths/register src/main.ts"
	},
	"dependencies": {
		"oag-shared": "*"
	}
}
